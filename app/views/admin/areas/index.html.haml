- title "Areas"

%p
  %em 
    Area data that is specific to every country. 

%table{table_defaults}
  %thead
    %tr
      %th id
      %th region_code
  - for area in @areas
    %tr{cycles}
      %td== ##{area.id}
      %td= link_to area.country, admin_area_path(:id => area.id)

