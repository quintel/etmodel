%h2 Comment Index

%table{table_defaults}
  %thead
    %tr
      %th Id
      %th Commentable
      %th Author
      %th Title
      %th Body
      %th Date
      %th
  %tbody
    - @comments.each do |c|
      %tr{cycles}
        %td= c.id
        %td
          = link_to [:admin, c.commentable] do
            = c.commentable_type
            = c.commentable_id
        %td= c.author_name
        %td= c.title
        %td= truncate(c.body)
        %td= I18n.l(c.created_at)
        %td
          = link_to 'edit', edit_admin_comment_path(c)
          = link_to 'delete', admin_comment_path(c), :method => :delete, :confirm => 'Are you sure?'
  %tfoot
    %tr
      %td{:colspan => 6}= paginate(@comments)