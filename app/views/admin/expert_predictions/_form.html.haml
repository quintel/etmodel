
%p 
  If a extra_key (gql statement) is defined, this will be fired after the slider has been updated

%div{:style => "width: 60%"}
  -semantic_form_for [:admin, @expert_prediction] do |form|
    = form.input :input_element_id, :collection => InputElement.all.select{|x|!x.slide.nil?}.map{|x| ["#{x.id} - #{x.name} - #{x.slide.name} - #{x.slide.controller_name}",x.id]}
    = form.inputs
    = form.buttons
  
  = link_to "back", admin_expert_predictions_path
