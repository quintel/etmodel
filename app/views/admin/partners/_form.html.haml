= simple_form_for [:admin, @partner] do |f|
  = f.input :name
  = f.input :url
  = f.input :country, :as => :select, :collection => Api::Area.all.map(&:country)
  = f.input :time
  = f.input :repeat_any_other
  = f.input :subheader
  = f.input :place, :as => :select, :collection => ['left', 'right']
  = f.input :long_name
  
  = f.button :submit