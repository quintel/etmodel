- title 'Expert predictions'

%table{table_defaults}
  %thead
    %tr
      %th.tal id
      %th.tal input element
      %th.tal Area
      %th.tal User
      %th.tal expert
      %th.tal curve_type
      %th.tal description
      %th.tal 


  %tbody
    - @predictions.each do |prediction|
      %tr{cycles}
        %td= link_to prediction.id, admin_prediction_path(prediction)
        %td
          = prediction.input_element.name
          = link_to prediction.input_element.id, edit_admin_input_element_path(prediction.input_element)
        %td= prediction.area
        %td= prediction.user.try(:name)
        %td= prediction.expert
        %td= prediction.curve_type
        %td= truncate(prediction.description)
        %td
          = link_to 'edit', edit_admin_prediction_path(prediction)
          |
          = link_to 'delete', admin_prediction_path(prediction), :method => :delete, :confirm => 'Are you sure?'

%p= link_to 'New', new_admin_prediction_path