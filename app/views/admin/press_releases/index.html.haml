- title "Press"

%p
  %em 
    All the press releases. 

%ul
  - for release in @releases
    %li
      = link_to release.title, [:admin, release]
      = link_to 'Edit', edit_admin_press_release_path(release)
      -#= link_to 'Destroy', [:admin, area], :confirm => 'Are you sure?', :method => :delete

%p= link_to "New Press release", new_admin_press_release_path

