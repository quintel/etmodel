%h2 New description

- semantic_form_for [:data, @description] do |form|
  - form.inputs 'English' do
    = form.inputs :short_content_en
    = form.input :content_en

  - form.inputs 'Dutch' do
    = form.inputs :short_content_nl
    = form.input :content_nl

  = form.input :describable_id, :as => :hidden
  = form.input :describable_type, :as => :hidden
  = form.buttons
  
= link_to "back", admin_descriptions_path