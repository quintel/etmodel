.slider{ :id => "slider-group-#{slider.share_group}-#{slider.key }", :'data-attrs' => slider.json_attributes }
  %div{ :id => "input_element_#{slider.key}" }
    .name
      .text= raw slider.translated_name
      .label
    .info-box{ :style => 'display: none' }
      .text= slider.sanitized_description
