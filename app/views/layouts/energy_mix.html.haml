!!! 5
%html.energy-mix
  %head
    %title= t("meta.title")
    %meta{:content => "text/html; charset=utf-8", "http-equiv" => "Content-Type" }
    %meta{:description => "#{t('meta.description')}" }
    = csrf_meta_tag

    = stylesheet_link_tag 'application', media: 'all'
    = javascript_include_tag 'application'

    :javascript
      I18n.defaultLocale = "#{I18n.default_locale}";
      I18n.locale = "#{I18n.locale}";

      window.energyMixSettings = #{energy_mix_client_settings.to_json.html_safe}

    = javascript_include_tag 'energy_mix'

    %style(media="print")
      @page { margin: 1.4cm 1cm 1cm; }

    = yield :head

  %body#energy-mix{ class: I18n.locale }
    = yield
