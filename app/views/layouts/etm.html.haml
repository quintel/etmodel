!!! Strict
%html{ :xmlns => "http://www.w3.org/1999/xhtml", :class => "#{"black_layout" if is_transitionprice?}"}
  %head
    %meta{:content => "text/html; charset=utf-8", "http-equiv" => "Content-Type" }
    %meta{:description => "#{t('meta.description')}" }
    %meta{:name=>"google-site-verification", :content=>"ILDmdAxQTYxcPyy969IJXq7YlXKzhGFfGCHqFi8fiWA"}
    %meta{:'http-equiv'=>"X-UA-Compatible", :content=>"IE=7"}
    = csrf_meta_tag
    %title= t("meta.title").force_encoding("UTF-8")
    = stylesheet_link_tag 'all',"vendor/jquery.fancybox-1.3.1","vendor/jquery.jqplot", 'vendor/formtastic', 'slider', :cache => asset_cache_name('all')
    = render "layouts/etm/javascript_globals"
    = javascript_include_tag 'underscore.min'
    = sprocket_layout_init :etm

    = javascript_include_tag 'i18n'
    = javascript_include_tag 'translations'

    = render "layouts/etm/javascript_init"
    = render "layouts/etm/javascript_unselectable"
    %script
      I18n.defaultLocale = "#{I18n.default_locale}";
      I18n.locale = "#{I18n.locale}";

  %body#model
    #wrapper
      #content
        - rounded_corners if is_transitionprice?

        = render "layouts/etm/title"
        = render "layouts/etm/content"
        .clear
        
        - if is_transitionprice?
          #footer
            = render "layouts/etm/constraints"
            = render "layouts/etm/footer"
          .clear

      #header
        = render "layouts/etm/header", :info_box => true, :save_button => APP_CONFIG[:save_button]
        = render "layouts/etm/tabs"
        
      - unless is_transitionprice?
        #footer
          = render "layouts/etm/constraints"
          = render "layouts/etm/footer"
      .clear

    %a{:id =>'feedback-tab',:href=>'/feedback',:class => "select_chart side_tabs #{I18n.locale}"}
    %a{:id =>'refman-tab',:href=>'http://refman.et-model.com',:class => "side_tabs #{I18n.locale}",:target=>"_blank"}
    - unless is_transitionprice?
      %a{:id =>'epn_module',:href=>'/pages/education',:class => "side_tabs #{I18n.locale}"}
    = render "layouts/etm/ga"
    
    //stick the footer at the bottom of the page if we're on an iPad/iPhone due to viewport/page bugs in mobile webkit
    %script 
      if(navigator.platform == 'iPad' || navigator.platform == 'iPhone' || navigator.platform == 'iPod')
      {
      $("#footer").css("position", "static");
      };
      window.App.call_api();
