- if has_active_scenario?
  = submit_tag "Start", :class => 'button button_align_right', :confirm => t('newscenario')
- else
  = submit_tag "Start", :class => 'button button_align_right'
  = hidden_field_tag :reset, '1'
