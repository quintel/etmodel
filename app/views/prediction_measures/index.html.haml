= stylesheet_link_tag 'prediction'

.prediction
  %h2
    = "Experts verwachten een adoptie van maximaal {max} in {year}. Jouw scenario ligt {location} de verwachtingen van experts. Hoe denk je dat de adoptie zal verlopen?"
  %br
  - @InputElement.predictions.each do |prediction|
    %br
    = prediction.description
    = "(#{prediction.user.try(:name)}) at #{prediction.created_at.strftime("%d-%m-%Y")}"
    %br
  
    - prediction.prediction_measures.each do |prediction_measure|
      %ul
        %li= prediction_measure.name
        %li= prediction_measure.cost
        %li= prediction_measure.impact
        %li= prediction_measure.year_start
        %li= prediction_measure.actor
      %br

  .nav_prediction
    %a.prediction(href="/predictions?input_element_id=#{@InputElement.id}") Vorige
    .close_prediction= "Close"