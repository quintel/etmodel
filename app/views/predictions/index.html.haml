= stylesheet_link_tag 'prediction'

.prediction
  %h2
    = "Experts verwachten een adoptie van maximaal {max} in {year}. Jouw scenario ligt {location} de verwachtingen van experts. Hoe denk je dat de adoptie zal verlopen?"
  %br
  - @InputElement.predictions.each do |prediction|
    %br
    = prediction.description
    = "(#{prediction.user.name}) at #{prediction.created_at.strftime("%d-%m-%Y")}"
    %br
    - prediction.prediction_values.each do |prediction_value|
      = prediction_value.year
      = prediction_value.best
      %br

  #prediction_chart

  .nav_prediction
    %a.prediction(href="/prediction_measures?input_element_id=#{@InputElement.id}") Volgende
    .close_prediction= "Close"