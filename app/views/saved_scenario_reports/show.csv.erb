<%=saved_scenario_url(id: @saved_scenario.id)%>
Category;Subcategory;Split;PresentValue;FutureValue;Unit
<%-@report_template.each do |category, value| %>
<%-value.each do |subcategory, subvalue| %>
<%-subvalue.each do |split, query| %>
<%=category%>;<%=subcategory%>;<%=split%>;<%=@queries[query].present.to_s.gsub('.',',')%>;<%=@queries[query].future.to_s.gsub('.',',')%>;<%=@queries[query].unit%>
<%-end%><%-end%><%-end%>
