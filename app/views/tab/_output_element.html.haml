#charts_wrapper
  #charts_holder
    - if @output_element.block_chart?
      - @blocks = @output_element.allowed_output_element_series
      = render "output_elements/block_chart"
    - else
      = render "output_elements/chart"
