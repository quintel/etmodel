location.hash = '';
$("#content").html('<%= escape_javascript render("show")%>');
var acc = new Accordion();
acc.setup();
window.input_elements.setup_input_elements();
if (history.pushState) {
  var stateObj = {url: "<%= request.fullpath %>"};
  history.pushState(stateObj, "ETM: <%= controller.controller_name %>", "<%= request.fullpath %>");
}
