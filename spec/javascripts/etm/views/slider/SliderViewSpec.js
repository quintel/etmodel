describe("SliderView", function() {

  var sliderView;

  beforeEach(function() {
    var holder = $('<div></div>');
    sliderView = new SliderView(holder);
  });

  it("should be able to set the max value", function() {
    
  });
});