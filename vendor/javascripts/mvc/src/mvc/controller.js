//= require 'mvc/event_dispatcher'

/**
 * Controller is the base class for all controllers. In a controller models
 * and views are glued together. 
 * 
 * @responsible Jaap van der Meer
 */
var Controller = EventDispatcher.extend({
  
});